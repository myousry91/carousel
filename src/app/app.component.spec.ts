import { TestBed, async } from '@angular/core/testing';
import { BulletComponent } from '../app/app.component';
describe('AppComponent', () => {
  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [
        BulletComponent
      ],
    }).compileComponents();
  }));
  
});
